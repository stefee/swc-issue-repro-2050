{
  "name": "swc-issue-repro-2050",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:repro-1": "cd repro-1 && swc server --config-file server/.swcrc --out-dir dist/server --copy-files",
    "build:repro-2": "cd repro-2/server && swc $PWD --out-dir ../dist/server --copy-files"
  },
  "dependencies": {
    "@swc/cli": "^0.1.49",
    "@swc/core": "^1.2.87"
  }
}
